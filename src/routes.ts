import { Router } from "express";

const router = Router();
const API_VERSION = "/api/v1";

// Aquí irían tus rutas específicas
// router.use(`${API_VERSION}/auth`, authRouter);
// router.use(`${API_VERSION}/users`, userRouter);

export default router;
